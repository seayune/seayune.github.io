<section>
  <div class="edge-alignment mx-auto p-8 text-zinc-700 text-sm flex flex-col w-full 2xl:max-w-7xl">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 lg:gap-12">
      <!-- Logo (left) -->
      <a href="/" class="block w-10 h-10 hover:opacity-80 transition-opacity">
        <img 
          src="/images/logo.png" 
          alt="Arthur Siyun Li" 
          class="h-10 w-auto"
          width="120"
          height="40"
          loading="lazy"
        >
      </a>

      <!-- Mobile Menu Button (right) -->
      <div class="md:hidden flex justify-end">
        <button 
          id="mobile-menu-button" 
          class="md:hidden text-zinc-700 hover:text-accent-600 flex items-center"
          style="height: 40px" <!-- Matches logo height -->
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>
      </div>

      <!-- Desktop Navigation (hidden on mobile) -->
      <div class="hidden md:flex flex-col md:items-end gap-y-0.3 font-serif text-base">
        <a class="hover:text-accent-600" href="/">about</a>
        <a class="hover:text-accent-600" href="/blog-one">listen</a>
        <a class="hover:text-accent-600" href="/blog-two">writing</a>
        <a class="hover:text-accent-600" href="/blog-three">gamedev</a>
        <a class="hover:text-accent-600" href="/contact">contact</a>
      </div>
    </div>

    <!-- Mobile Dropdown (hidden by default) -->
    <div id="mobile-menu" class="hidden md:hidden mt-4 font-serif text-base">
      <div class="flex flex-col gap-0.3 items-end">
        <a class="hover:text-accent-600" href="/">about</a>
        <a class="hover:text-accent-600" href="/blog-one">listen</a>
        <a class="hover:text-accent-600" href="/blog-two">writing</a>
        <a class="hover:text-accent-600" href="/blog-three">gamedev</a>
        <a class="hover:text-accent-600" href="/contact">contact</a>
      </div>
    </div>
  </div>
</section>

<script>
  // Toggle mobile menu
  document.getElementById('mobile-menu-button').addEventListener('click', function() {
    const menu = document.getElementById('mobile-menu');
    menu.classList.toggle('hidden');
  });
</script>